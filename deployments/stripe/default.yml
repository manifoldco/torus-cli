name: stripe
description: "Public production instance of Stripe"
type: subscription
locations:
  - name: api
    url: "https://api.stripe.com"

descriptors:
    default: 0.0.1
    supported:
        - version: 0.0.1
          url: "file://$ARIGATO_INSTALL/descriptors/stripe/service.yml"
